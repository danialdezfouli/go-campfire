package tests

import (
	"errors"
	"testing"
)

func TestCreateChannel(t *testing.T) {
	err := errors.New("TestCreateChannel")
	t.Fatal(err)
}

func TestUpdateChannel(t *testing.T) {
	err := errors.New("TestUpdateChannel")
	t.Fatal(err)
}
